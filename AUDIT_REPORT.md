# AUDIT REPORT — SAP Kontrollscan V8.8.4

## 1) Краткое резюме

### Что хорошо
- Есть полноценный SPA/double-init guard через `window[GUARD_KEY]` с принудительным `teardown('reinit')` перед повторной инициализацией.
- Жизненный цикл в целом полный: таймеры, observer, UI-узлы и большинство глобальных listener-ов снимаются в `teardown`.
- Каскад ошибок структурирован: `ToCount < 0` → UI5 MessageManager → row status fallback.
- Антиспам звука реализован сразу несколькими барьерами: `MIN_BEEP_GAP_MS`, `pendingBeep` + TTL, `audio unlock`.
- Есть оптимизации DOM-скана: `scanCache`, `tableDirty`, throttling observer.

### Что опасно
- **P0:** `setupAudioUnlockHooks()` вешает `pointerdown/keydown/touchstart` listener-ы, но `teardown` их не снимает, если пользователь не сделал первый unlock-жест. Это создаёт утечки/дубли колбэков при SPA-реинициализациях.
- **P1:** `stableRowKey()` может коллидировать (например, одинаковый Material в нескольких строках), что влияет на детекцию «новизны» ошибок и может давать пропуски/ложные beep-переходы.
- **P1:** жёсткая привязка состояния к DOM (`page/title` селекторы, regex заголовка) может давать ложные переходы `GREEN→WHITE/YELLOW` во время UI5 rerender и тем самым сбрасывать сигнатуру/счётчик.

### Что срочно чинить
1. Добавить явное хранение и снятие unlock-listener-ов в `teardown` (P0).
2. Усилить стабильный ключ строки (Material + партия/позиция/текст статуса), чтобы убрать коллизии (P1).
3. Добавить debounce/гистерезис при определении `baseState`, чтобы не сбрасываться в WHITE/YELLOW на кратких DOM-провалах (P1).

---

## 2) Карта модулей скрипта

| Подсистема | Ключевые функции | Ответственность |
|---|---|---|
| Bootstrap / Guard | top-level guard, `init`, `teardown` | Защита от double-init, запуск и финальная очистка |
| Selectors/DOM utils | `SELECTORS`, `q1`, `qAll`, `findFirst` | Унификация поиска SAP/UI элементов |
| Settings | `loadSettings`, `saveSettings`, `clampNum` | Чтение/сохранение настроек в localStorage |
| UI layer | `injectStyle`, `ensureUiLayer`, `ensureIndicator`, `ensureToast`, `ensureSettingsPanel`, `toggleSettings` | Индикатор, toast, панель настроек, CSS-инъекция |
| Audio | `ensureAudioCtx`, `unlockAudioIfNeeded`, `setupAudioUnlockHooks`, `playBeep`, `closeAudioCtx` | WebAudio-сигналы, anti-spam, unlock flow |
| UI5 integration | `safeGetCore`, `getUi5MessagesData`, `ui5GetErrorState` | Чтение MessageManager сообщений |
| State machine | `getBaseScreenState`, `setIndicatorState`, `lightCleanupOnLeaveHu`, `tick` | WHITE/YELLOW/GREEN + ERROR overlay |
| Error detection | `parseLocaleNumber`, `scanErrorsUnified`, `matchesErrorText`, `buildErrorSignature` | Каскад поиска ошибок и сигнатуры новизны |
| Growing/Weitere | `parseTriggerXY`, `isWeitereNeeded`, `getHuTableControl`, `ui5LoadMore`, `startRampIfAllowed`, `scheduleRampTick`, `stopRamp` | Автодогрузка таблицы |
| Perf/Observer | `setupObserverForTable`, `detachObserver`, `rebuildScanCacheIfNeeded` | Обновление `tableDirty`, throttling scan |
| Cleanup helpers | `removeUiElements`, `removeGlobalListeners`, `clearAllHighlights`, `stopPulseSync` | Комплексный teardown |

---

## 3) Матрица проблем (BUG/RISK)

См. полный реестр в `ISSUES_MATRIX.md`.

### P0
- BUG-001: Утечка unlock-listeners audio (не снимаются в teardown, если unlock не произошёл).

### P1
- RISK-001: Коллизии `stableRowKey()` и ошибки новизны beep.
- RISK-002: Флаппинг базового состояния при кратковременном отсутствии DOM-узлов HU.
- RISK-003: Частичное undercount/маскирование при приоритете веток каскада (`ToCount` доминирует над UI5/Status).
- RISK-004: `setTimeout(... weitereRunning=false)` не учитывается в teardown-пуле таймеров.

### P2
- RISK-005: Несоответствие метаданных (`@name`/filename 8.8.4, `@version` = 8.8.5).
- RISK-006: Широкие `catch (_) {}` усложняют диагностику прод-инцидентов.

---

## 4) Рекомендации FIX (с риском внедрения)

- **FIX-001 (низкий риск):** хранить ссылки на unlock-handler в `HANDLERS`, снимать в `removeGlobalListeners`, ставить guard `if (destroyed) return` внутри handler.
- **FIX-002 (средний риск):** расширить `stableRowKey`: `MAT + batch + alt-id + trimmed status`, добавить детектор коллизий (debug metric).
- **FIX-003 (средний риск):** добавить debounce (например 2 последовательных тика) перед переходом `GREEN→WHITE/YELLOW`.
- **FIX-004 (низкий риск):** агрегировать источники ошибок и разделять `displayCount`/`signature` для исключения потери не-ToCount ошибок.
- **FIX-005 (низкий риск):** заносить все ad-hoc `setTimeout` в общий реестр таймеров для гарантированного teardown.
- **FIX-006 (низкий риск):** добавить условный debug-лог (флаг) в ключевых catch-блоках.

---

## 5) Регрессионные тесты (чек-лист)

Полный план и PASS/FAIL — в `TEST_PLAN.md`.

Критичные группы:
1. Машина состояний WHITE/YELLOW/GREEN + ERROR overlay.
2. Каскад детекции (`ToCount`, row-status fallback, UI5 MessageManager).
3. Новизна ошибок и правила beep (рост/снижение/замена).
4. Ramp/Weitere: stop-условия, no-grow streak, userBusy.
5. Teardown при SPA-навигации, rapid switching, pagehide/beforeunload.

---

## 6) Диаграмма состояний (формализация)

```text
WHITE (вне HU)
  └─(HU page + title found, но regex HU не совпал)→ YELLOW
YELLOW (HU, контроль не начат)
  └─(regex ": <n> (<n>)" совпал)→ GREEN
GREEN (контроль начат)
  ├─(hasError=false)→ GREEN/🟢
  └─(hasError=true) → GREEN/🔴

Из любого состояния при потере page/title селекторов → WHITE
```

### Конфликтующие ветки
- При кратковременном DOM-rerender: `GREEN` может ложно уйти в `WHITE/YELLOW`, выполняется `lightCleanupOnLeaveHu`, сбрасываются `lastErrorSignature`/`lastActiveErrorCount`, что влияет на beep-новизну после возврата.

---

## 7) Таблица “источник → условия → точность → риски”

| Источник | Условие активации | Точность | Риски ложных срабатываний / пропусков |
|---|---|---|---|
| ToCount numeric | `toCountValid && toCountNegCount>0` | Высокая при корректном парсинге | Пропуск, если формат числа экзотический/непарсибельный |
| Row status text | `!rowToCountParsed || ui5.active`, regex по `ERROR_PATTERNS` | Средняя | Локализационные вариации текста, неполные паттерны |
| UI5 MessageManager | `ui5GetErrorState().active` | Средняя/высокая | Непривязанные к текущей строке сообщения, stale messages |

---

## 8) Формальная спецификация beep (ожидаемое поведение)

Beep должен срабатывать, когда одновременно:
1. Есть активная ошибка (`hasError=true`),
2. Сигнатура изменилась (`sig !== lastErrorSignature`),
3. `activeCount >= lastActiveErrorCount` (новые/увеличившиеся/заменившиеся при том же количестве),
4. `MIN_BEEP_GAP_MS` выдержан,
5. pending-событие не протухло (`age <= PENDING_BEEP_TTL_MS`),
6. audio unlocked (или будет доиграно при unlock в пределах TTL).

**Не должен срабатывать:** когда ошибок стало меньше, даже если они ещё есть.

Найденные расхождения:
- Из-за возможной коллизии row key (RISK-001) «замена» набора ошибок может не детектироваться как новая.
- При state-flap (RISK-002) после искусственного reset может возникать повторный beep как «новая» ошибка.

---

## 9) Зависимости и side-effects

- **DOM selectors:** все ключи из `SELECTORS` (HU page/title/table rows/toCount/status/weitere + собственные UI ids).
- **UI5 объекты:** `sap.ui.getCore()`, `MessageManager`, message model data, table control (`triggerGrowing/_triggerGrowing/_oGrowingDelegate.requestNewPage`).
- **Storage:** `localStorage['kc_settings_v1']`.
- **Web APIs:** `MutationObserver`, `setTimeout`, `setInterval`, `AudioContext`, window listeners (`keydown/mousedown/touchstart/wheel/pointerdown/beforeunload/pagehide`).

