# ISSUES MATRIX — SAP Kontrollscan V8.8.4

| ID | Где (функция/блок) | Симптом | Причина | Условия воспроизведения | Влияние | Приоритет | Предложение фикса | Риск фикса |
|---|---|---|---|---|---|---|---|---|
| BUG-001 | `setupAudioUnlockHooks`, `removeGlobalListeners`, `teardown` | После SPA-реинициализаций растёт число unlock-callback, возможны дубли побочных действий | Unlock-listener-ы (`pointerdown/keydown/touchstart`) создаются локально и не снимаются в teardown, если `once` не был вызван | Открыть HU, не кликать по странице, перейти SPA, вернуться несколько раз | Потенциальная утечка + дублирование реакций | P0 | FIX-001: хранить refs этих listener-ов в `HANDLERS` и снимать всегда в teardown | Низкий |
| RISK-001 | `stableRowKey`, `scanErrorsUnified`, `buildErrorSignature` | Пропуск beep на «новых» ошибках или ложные повторные beep | Ключ строки основан преимущественно на Material; при дублях Material коллизии возможны | Таблица с несколькими строками одного материала, исчезновение/перестановка строк | Некорректная логика новизны/замены ошибок | P1 | FIX-002: усиленный row key (material+batch/pos/status fingerprint) | Средний |
| RISK-002 | `getBaseScreenState`, `tick`, `lightCleanupOnLeaveHu` | Краткий flicker в ⚪/🟡 и повторный beep после возврата в GREEN | Жёсткая зависимость от мгновенного наличия title/page в DOM; на rerender происходит reset состояния | Быстрые UI5 перерисовки, переключение вкладок внутри SAP | Нестабильность индикации + возможные ложные звуки | P1 | FIX-003: debounce/hysteresis для ухода из GREEN | Средний |
| RISK-003 | Каскад в `tick` (`if toCount ... else if ui5 ... else if dom...`) | Подсчёт активных ошибок может быть неполным при одновременных источниках | Жёсткий приоритет источников, а не объединение | Есть отрицательный ToCount и параллельно отдельные UI5/Status ошибки | Недооценка объёма проблемы, mismatch count/signature | P1 | FIX-004: агрегировать источники с дедупликацией и раздельными метриками | Средний |
| RISK-004 | `startRampIfAllowed` | Неучтённый timeout существует после teardown | `setTimeout(() => weitereRunning=false, 250)` не хранится и не чистится | Teardown сразу после старта ramp | Микро-утечка таймера/недетерминизм флагов | P1 | FIX-005: регистрировать timeout id в пул таймеров teardown | Низкий |
| RISK-005 | Userscript metadata header | Версионная путаница в сопровождении | filename/name = 8.8.4, но `@version`=8.8.5 | Ручная верификация релизов | Ошибки эксплуатации/поддержки | P2 | FIX-006: синхронизировать version/name/file naming policy | Низкий |
| RISK-006 | Множественные `catch (_) {}` | Трудно расследовать edge-case баги | Полное подавление исключений без telemetry/debug | Нестандартная среда браузера/UI5 API дрейф | Невидимые отказы и «тихие» деградации | P2 | FIX-007: условный debug-logging (флаг) в ключевых catch | Низкий |
| ASSUMPTION-001 | `ui5GetErrorState` + MessageManager | Возможны stale UI5 messages не относящиеся к текущей HU | Нет явной фильтрации по текущему контексту HU/таблице | Если MessageManager хранит сообщения прошлых действий | Потенциальные ложные ERROR state | P2 | FIX-008: фильтрация по target/path/контексту страницы при доступности данных | Средний |

