📄 SAP Kontrollscan – FINAL SPEC v1.0

(Строгая спецификация. Без допущений. Базовая архитектура зафиксирована.)

1️⃣ ОБЛАСТЬ РАБОТЫ
1.1 Экран

Скрипт работает только на странице HU-Inhalt prüfen.

Состояния вне экрана:

Статус: ⚪

Сканирование: ❌

MutationObserver: ❌

Нажатия “Weitere”: ❌

Поиск ошибок: ❌

1.2 Размер списка

Минимум строк: 1

Максимум строк: 50–70

200+ строк не рассматриваются

Growing через “Weitere” обязателен

2️⃣ СОСТОЯНИЯ
Статус	Условие	Сканирование
⚪	Не HU экран	Нет
🟡	HU экран, но контроль не начат	Нет
🟢	Контроль начат	Да
🔴	Есть активные ошибки	Да
Приоритет:

🔴 имеет абсолютный приоритет над 🟢 и 🟡.

3️⃣ ОПРЕДЕЛЕНИЕ “КОНТРОЛЬ НАЧАТ”

Контроль считается начатым, если выполняется порядок:

В заголовке есть HU номер

Есть строки таблицы

В строках есть ToCount

Особый случай:

Таблица пустая

HU номер всё ещё отображается

→ Состояние 🟢
(Все позиции проверены, кнопка подтверждения SAP скрипта не касается)

4️⃣ ЛОГИКА ОШИБОК
4.1 Главный источник истины
🔴 Перескан
ToCount < 0 → ошибка

Поддерживаемые форматы:

-1

-0,15

-0.15

с пробелами

локальные разделители

4.2 Парсинг
Значение	Интерпретация
< 0	🔴 ошибка
= 0	позиция закрыта
> 0	требуется досканировать
не распарсилось	fallback
4.3 Row-level fallback

Если ToCount не распарсился:

Проверяем UI5 MessageManager

Проверяем текст статуса строки

Фолбэк применяется только к этой строке,
не ко всей таблице.

5️⃣ СИГНАЛ
5.1 Когда сигнал звучит

Сигнал воспроизводится только если:

появилась новая строка с ошибкой

или строка с ошибкой была исправлена (исчезла)
и затем появилась снова

Сигнал не воспроизводится если:

изменилась величина ошибки в уже существующей строке
(например: -1 → -2)

количество ошибок увеличилось за счёт изменения значения
уже существующей строки

количество ошибок уменьшилось

ошибки остаются активными без появления новых строк

Техническая формулировка
Сигнал воспроизводится только при появлении
строки, отсутствующей в предыдущем множестве
активных ошибок.

5.2 Интервал

Минимальный интервал между сигналами:
1 секунда

5.3 Поведение

Один короткий фиксированный сигнал (LITE)

Нет постоянной сирены

Нет повторения при активной 🔴

6️⃣ TOAST

Появляется при обнаружении ошибки.

Содержит:

текст предупреждения

количество активных ошибок

Закрывается:

кнопкой OK

Если ошибки исчезли:

toast автоматически скрывается

статус возвращается в 🟢

Клик по индикатору:

повторно показывает toast (если ошибки есть)

7️⃣ HIGHLIGHT

Подсвечиваются только строки с ошибкой.

Требования:

только background / box-shadow

не менять высоту строки

не менять position

не менять layout

при исчезновении строки подсветка исчезает

8️⃣ “WEITERE”
8.1 Когда нажимать

Только если:

контроль начат (🟢 или 🔴)

есть кнопка “Weitere”

есть формат [x/y]

x < y

не превышен лимит попыток

8.2 Ограничения

Не реагировать на движение мыши (вибрация рабочего места)

Останавливать попытки если строки не увеличиваются

Не превышать лимит

9️⃣ ПРОИЗВОДИТЕЛЬНОСТЬ

Требования:

ПК не должен подвисать

Нет агрессивного FAST_TICK

Observer ограничен HU таблицей

Кеш строк разрешён

Перестройка кеша только при tableDirty

Нет постоянного пересканирования вне HU

🔟 АРХИТЕКТУРА
10.1 Версии

Поддерживаются:

FULL (с настройками)

LITE (фиксированная логика)

10.2 Обязательные элементы

SPA-safe teardown

Защита от двойной инициализации

Централизованные селекторы

Изоляция UI от SAP DOM

1️⃣1️⃣ СКРИПТ НЕ ДОЛЖЕН

❌ Изменять SAP данные
❌ Вызывать SAP API
❌ Кликать строки
❌ Влиять на бизнес-логику
❌ Ломать стандартные кнопки
❌ Скроллить видимо пользователю

Разрешено:

Кликать только кнопку “Weitere”

1️⃣2️⃣ НЕРЕЛЕВАНТНО

Не учитывать:

Статистику смен

Производственные отчёты

Тип упаковки

Длину HU

Логирование

Историю ошибок