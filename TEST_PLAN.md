# TEST PLAN — SAP Kontrollscan V8.8.4

## Формат сценариев
- **Setup:** предусловия.
- **Steps:** действия.
- **PASS:** ожидаемое поведение.
- **FAIL:** что считается провалом.

---

## 1) HU-страница / не HU

### TC-001: Не HU-страница
- Setup: открыть любой экран SAP без `--pageHuControl`/`--titleTableHuControl`.
- Steps: подождать 2–3 тика.
- PASS: индикатор ⚪, toast скрыт, подсветок нет, ramp не стартует.
- FAIL: 🟡/🟢/🔴 в нецелевом экране или попытки автокликов.

### TC-002: HU страница, контроль не начат
- Setup: HU экран открыт, regex `: <n> (<n>)` в title ещё не выполнен.
- Steps: наблюдать 2–3 тика.
- PASS: индикатор 🟡, toast скрыт, beep нет.
- FAIL: переход в 🟢/🔴 без признаков начала контроля.

### TC-003: HU страница, контроль начат
- Setup: title удовлетворяет regex `: <digits> (<digits>)`.
- Steps: наблюдать 1–2 тика.
- PASS: индикатор 🟢 при отсутствии ошибок.
- FAIL: остаётся 🟡/⚪.

---

## 2) Ошибки 0 / 1 / 2 и динамика

### TC-010: 0 ошибок
- Setup: GREEN + все ToCount >=0, нет UI5/Status ошибок.
- Steps: подождать 2 тика.
- PASS: 🟢, нет toast, нет beep, нет подсветки.
- FAIL: ложный 🔴/toast/beep.

### TC-011: 1 ошибка ToCount
- Setup: GREEN + одна строка с ToCount < 0.
- Steps: дождаться первого срабатывания.
- PASS: 🔴, toast “Aktive Fehler: 1 · Zum Nachzählen”, один beep (с учётом unlock).
- FAIL: отсутствие индикации или многократный beep-спам.

### TC-012: 2 ошибки ToCount
- Setup: две строки ToCount < 0.
- Steps: наблюдать переход 1→2.
- PASS: счётчик 2, beep допускается как «увеличение».
- FAIL: счётчик не меняется/нет beep при росте.

### TC-013: Ошибок стало меньше, но >0
- Setup: состояние 2 ошибок → исправить до 1.
- Steps: наблюдать переход 2→1.
- PASS: beep **не** звучит, 🔴 остаётся, счётчик обновляется.
- FAIL: звучит beep при уменьшении.

### TC-014: Ошибки полностью ушли
- Setup: было >0, затем 0.
- Steps: наблюдать 1–2 тика.
- PASS: 🟢, toast скрыт, pending beep очищен, last counters reset.
- FAIL: остаётся 🔴 или повторно всплывает toast.

---

## 3) ToCount парсинг / fallback

### TC-020: Парсинг локалей
- Setup: значения `-0,15`, `-1.234,50`, `-1,234.50`, `-2 PAK`.
- Steps: прогнать скан.
- PASS: отрицательные корректно детектируются как ошибки.
- FAIL: отрицательные не распознаны.

### TC-021: ToCount не парсится (`nn`)
- Setup: в строке ToCount текст `nn`, status = ошибка.
- Steps: скан.
- PASS: строка помечается через row-status fallback.
- FAIL: строка пропущена.

### TC-022: ToCount не парсится, status чистый
- Setup: ToCount `nn`, status без error-паттерна.
- Steps: скан.
- PASS: строка не считается ошибочной.
- FAIL: ложное срабатывание.

---

## 4) UI5 MessageManager

### TC-030: Есть UI5 error message
- Setup: активное сообщение с `Mengendifferenz...`.
- Steps: скан в GREEN.
- PASS: 🔴, source `UI5` (если нет ToCount NEG приоритета), beep по правилам новизны.
- FAIL: UI5 ошибка игнорируется.

### TC-031: UI5 + ToCount одновременно
- Setup: одновременно UI5 message и ToCount<0.
- Steps: скан.
- PASS: приоритет ветки ToCount (текущее поведение), задокументирован undercount риск.
- FAIL: нестабильный флаппинг между источниками на каждом тике.

---

## 5) “Weitere” (догрузка)

### TC-040: Trigger есть, `[x/y]`, x<y
- Setup: GREEN, trigger доступен.
- Steps: наблюдать ramp цикл.
- PASS: запуск ограниченных попыток, respect cooldown/tries/no-grow stop.
- FAIL: бесконечные попытки или спам-клики.

### TC-041: Trigger нет / x>=y
- Setup: убрать trigger или сделать `[y/y]`.
- Steps: наблюдать.
- PASS: ramp не стартует.
- FAIL: попытки догрузки без необходимости.

### TC-042: User busy во время ramp
- Setup: ramp активен, пользователь интенсивно кликает/скроллит.
- Steps: генерировать `keydown/mousedown/wheel`.
- PASS: ramp прекращается (текущее поведение).
- FAIL: ramp продолжает вмешиваться в UX.

---

## 6) SPA переходы / быстрый переход / закрытие

### TC-050: SPA переход между экранами
- Setup: перейти HU → другой экран → HU без reload.
- Steps: повторить 3–5 раз.
- PASS: нет дублей UI, один индикатор, не растут observer/listener эффекты.
- FAIL: множественные панели/тосты/дубли звуков.

### TC-051: Быстрый переход сразу после старта ramp
- Setup: спровоцировать ramp и сразу уйти со страницы.
- Steps: наблюдать консоль/поведение.
- PASS: нет ошибок после teardown.
- FAIL: пост-эффекты от «висячих» timeout.

### TC-052: Закрытие вкладки / pagehide
- Setup: активная ошибка + открытая панель.
- Steps: закрыть вкладку/навигация назад.
- PASS: `teardown` выполняется без исключений.
- FAIL: утечки/ошибки в console на unload.

---

## 7) Пользовательские действия

### TC-060: Клик по индикатору при ошибке
- Setup: 🔴 и toast закрыт кнопкой OK.
- Steps: клик по индикатору.
- PASS: toast показывается снова.
- FAIL: клик не восстанавливает toast.

### TC-061: Contextmenu индикатора
- Setup: любой state.
- Steps: правый клик по индикатору.
- PASS: открывается settings panel.
- FAIL: открывается браузерное меню вместо панели.

### TC-062: Клик вне settings
- Setup: settings открыт.
- Steps: pointerdown вне panel/indicator.
- PASS: settings закрывается.
- FAIL: panel «залипает».

---

## 8) Производительность и стабильность

### TC-070: Нагрузка на длинной таблице
- Setup: 300+ строк в `tableHuItems`.
- Steps: 2–3 минуты наблюдения.
- PASS: нет заметных фризов; observer-throttle и scanCache уменьшают лишние пересканы.
- FAIL: постоянные лаги UI.

### TC-071: Table rerender storms
- Setup: частые DOM-мутации таблицы.
- Steps: инициировать серию обновлений.
- PASS: `tableDirty` поднимается, но нет бесконечного горячего цикла.
- FAIL: цикл перманентно в FAST без пользы.

